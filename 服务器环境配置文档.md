# 服务器环境配置文档

## 1. 后端环境配置（Python + FastAPI）

### 1.1 环境要求
- Python 3.8+
- pip 20.0+

### 1.2 配置步骤

#### 1.2.1 创建虚拟环境
```bash
# 进入项目根目录
cd /path/to/vertical_website

# 进入backend目录
cd backend

# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
# Mac/Linux
source venv/bin/activate
# Windows
# venv\Scripts\activate
```

#### 1.2.2 安装依赖
```bash
# 安装FastAPI和Uvicorn
pip install fastapi uvicorn

# 安装其他依赖（如果有）
pip install -r requirements.txt
```

#### 1.2.3 启动后端服务
```bash
# 使用Uvicorn启动FastAPI应用
uvicorn main:app --reload

# 生产环境启动方式
# uvicorn main:app --host 0.0.0.0 --port 8000
```

### 1.3 测试后端服务
```bash
# 测试健康检查接口
curl http://localhost:8000/health

# 测试内容列表接口
curl http://localhost:8000/api/content/list
```

## 2. 前端环境配置（Vue 3 + Vite）

### 2.1 环境要求
- Node.js 18+
- npm 9+

### 2.2 配置步骤

#### 2.2.1 安装Node.js
- 访问 [Node.js官网](https://nodejs.org/) 下载并安装最新版Node.js
- 验证安装：
  ```bash
  node -v
  npm -v
  ```

#### 2.2.2 安装依赖
```bash
# 进入项目根目录
cd /path/to/vertical_website

# 进入frontend目录
cd frontend

# 安装项目依赖
npm install
```

#### 2.2.3 启动前端开发服务器
```bash
# 启动开发服务器
npm run dev

# 生产环境构建
# npm run build

# 预览生产构建
# npm run preview
```

### 2.3 测试前端服务
- 访问 http://localhost:5173
- 使用curl测试：
  ```bash
  curl -I http://localhost:5173/
  ```

## 3. 环境变量配置

### 3.1 后端环境变量
在backend目录下创建.env文件：
```env
# FastAPI配置
APP_NAME=垂直内容聚合平台
APP_VERSION=1.0.0

# 数据库配置（使用PostgreSQL）
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# 服务器配置
HOST=0.0.0.0
PORT=8000
```

### 3.2 前端环境变量
在frontend目录下创建.env文件：
```env
# Vite配置
VITE_APP_NAME=垂直内容聚合平台
VITE_API_BASE_URL=http://localhost:8000
```

## 4. 常见问题排查

### 4.1 后端问题
- **端口被占用**：使用`lsof -i :8000`查看占用端口的进程，然后使用`kill <PID>`结束进程
- **依赖安装失败**：检查网络连接，尝试使用国内镜像源：`pip install -i https://pypi.tuna.tsinghua.edu.cn/simple fastapi uvicorn`

### 4.2 前端问题
- **npm依赖安装失败**：检查网络连接，尝试使用国内镜像源：`npm config set registry https://registry.npmmirror.com`
- **端口被占用**：修改vite.config.js中的端口配置

## 5. 部署建议

### 5.1 后端部署
- 使用Gunicorn + Uvicorn作为生产服务器
- 配置Nginx作为反向代理
- 使用systemd或PM2管理进程

### 5.2 前端部署
- 构建生产版本：`npm run build`
- 将dist目录部署到Nginx或Apache服务器
- 配置Nginx反向代理API请求

# IDE远程服务器连接配置

## 1. SFTP/SSH连接配置

### 1.1 配置步骤
1. 在IDE中打开设置（Settings）
2. 搜索"SFTP"或"Remote Development"
3. 点击"Add New SFTP Connection"或"Add New SSH Connection"
4. 填写连接信息：
   - Host: 服务器IP地址
   - Port: 22（SSH默认端口）
   - Username: 服务器用户名
   - Password: 服务器密码（或使用SSH密钥）
   - Remote Path: 服务器上的项目目录路径（如：/home/user/vertical_website）

### 1.2 配置文件示例（以VS Code为例）
在.vscode目录下创建sftp.json文件：
```json
{
  "name": "Remote Server",
  "host": "your-server-ip",
  "protocol": "sftp",
  "port": 22,
  "username": "your-username",
  "password": "your-password",
  "remotePath": "/home/user/vertical_website",
  "uploadOnSave": true,
  "syncMode": "update",
  "watcher": {
    "files": "**/*",
    "autoUpload": true,
    "autoDelete": true
  }
}
```

### 1.3 使用说明
- 保存文件时自动上传到服务器
- 可以手动同步本地和远程文件
- 直接在IDE中编辑远程文件
- 支持终端直接连接到远程服务器

## 2. 注意事项
- 确保服务器上已安装所需的开发环境
- 配置适当的文件权限
- 使用SSH密钥认证提高安全性
- 定期备份项目文件
- 注意网络连接稳定性